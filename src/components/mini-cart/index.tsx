import React from 'react';
import { Wrapper, CartIcon } from './styles';

const MiniCart: React.FC = () => {
  return (
    <Wrapper>
      <CartIcon />
    </Wrapper>
  );
};

export default MiniCart;
